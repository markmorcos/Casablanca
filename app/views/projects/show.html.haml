- content_for :title, @project.name

%section
  .row
    .columns
      %h1= @project.name
      = image_tag @project.image, class: float("left")
      = simple_format(@project.description)
      .clear
